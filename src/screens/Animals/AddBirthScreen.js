import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddBirthScreen = () => {
  return (
    <View>
      <Text>AddBirthScreen</Text>
    </View>
  );
};

export default AddBirthScreen;

const styles = StyleSheet.create({});
